# Server & App Settings
PORT=3000
NODE_ENV=production
APP_URL=https://kemenagoki.id

# Database MySQL Configuration (Hostinger / cPanel / Cloud)
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=

# Email / SMTP Configuration
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Humas Kemenag OKI" <humas@kemenagoki.id>

# Security / Admin Default Password (Opsional)
ADMIN_PASSWORD=kemenagoki123
