        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }V        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }H        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }H        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }5        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }8        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }7        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }@        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }V        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }9        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }[        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }]        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }!        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }!        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }H        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }*        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }*        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }Q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }&        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }&        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }[        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }]        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }[        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }]        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }H        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }H        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }5        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }5        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }G        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }!        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }!        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }!        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }W        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }W        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }+        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }+        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }*        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }9        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }[        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }^        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }Z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }9        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }]        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }G        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }W        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }G        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }3        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }|        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }8        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }8        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }8        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }8        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }G        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }G        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }G        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }W        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }!        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }P        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }>        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }h        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }1        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }0        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }2        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }K        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }C        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }/        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }j        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }-        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }$        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }x        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }`        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }v        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }b        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }B        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }6        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }4        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }z        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }:        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }=        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }k        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }"        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }M        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }Q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }&        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }&        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }V        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }[        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }J        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }]        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }w        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }q        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }R        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }I        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }T        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }V        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }A        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }L        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }U        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }E        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }?        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }[        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }m        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }u        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }p        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }a        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }d        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }_        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }'        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        },        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }J        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }S        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }O        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }N        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }.        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }t        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }r        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }n        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }f        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }y        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }(        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }g        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }F        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }i        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }D        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }o        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }c        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }]        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        })        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        };        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }}        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }l        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }s        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }e        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }{        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }
        // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const isOg = f.isOg || (f.id && f.id.includes("-og."));
        if (type === "og_image" && !isOg) {
          return;
        } else if (type !== "og_image" && isOg) {
          return;
        }         // Filter OG images based on type
        const 